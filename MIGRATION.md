# Cyber Zen Tools - Go 到 Rust 迁移指南

本文档说明如何从 Go 版本迁移到 Rust 版本。

## 迁移概述

Rust 版本完全重构了原有 Go 代码，保持了所有功能的兼容性，同时提供了更好的性能和类型安全。

## 前置要求

### 1. 安装 Rust 环境

**macOS 和 Linux:**
```bash
# 使用官方安装脚本（推荐）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

**Linux:**
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

安装完成后，重启终端或运行：
```bash
source $HOME/.cargo/env
```

### 2. 验证 Rust 安装

```bash
rustc --version
cargo --version
```

## 迁移步骤

### 步骤 1: 进入 Rust 项目目录

```bash
cd /Users/robert/SelfMine/MyProject/cyber-zen-tools/cyber-zen-tools-rust
```

### 步骤 2: 构建项目

```bash
# 开发模式构建（包含调试信息）
cargo build

# 发布模式构建（优化版本）
cargo build --release
```

构建完成后，二进制文件位于：
- 开发版：`target/debug/cyber-zen-tools`
- 发布版：`target/release/cyber-zen-tools`

### 步骤 3: 测试功能

```bash
# 测试状态命令
cargo run -- status

# 测试 Git 提交（需要 Git 仓库）
cargo run -- gcm

# 测试图片压缩
cargo run -- compress --src "test.jpg" --rate 0.8

# 测试静态服务器
cargo run -- server --port 3000
```

### 步骤 4: 安装到系统（可选）

**方式 1: 使用 cargo install**
```bash
cargo install --path .
```

**方式 2: 手动安装**
```bash
# 复制二进制文件
sudo cp target/release/cyber-zen-tools /usr/local/bin/cyber-zen

# 设置执行权限
sudo chmod +x /usr/local/bin/cyber-zen
```

**方式 3: 创建符号链接**
```bash
# 创建符号链接（推荐，便于更新）
sudo ln -sf $(pwd)/target/release/cyber-zen-tools /usr/local/bin/cyber-zen
```

### 步骤 5: 验证安装

```bash
# 检查命令是否可用
cyber-zen --version
cyber-zen --help

# 测试各个命令
cyber-zen status
```

## 配置文件兼容性

Rust 版本完全兼容 Go 版本的配置文件：

- ✅ `configs/file-types.yaml` - 文件类型配置
- ✅ `configs/categories.yaml` - 文件分类配置
- ✅ `configs/commit-templates.yaml` - Commit 模板配置

配置文件查找优先级（与 Go 版本相同）：
1. 项目目录 (`./configs/`)
2. 可执行文件同目录 (`<exe_dir>/configs/`)
3. 用户主目录 (`~/.cyber-zen/configs/`)

**无需修改配置文件，可直接使用！**

## 功能对比

| 功能 | Go 版本 | Rust 版本 | 状态 |
|------|---------|-----------|------|
| Git 提交和推送 | ✅ | ✅ | 完全兼容 |
| 智能 Commit Message 生成 | ✅ | ✅ | 完全兼容 |
| 图片压缩 | ✅ | ✅ | 完全兼容 |
| 静态文件服务器 | ✅ | ✅ | 完全兼容 |
| 状态显示 | ✅ | ✅ | 完全兼容 |
| 卸载功能 | ✅ | ✅ | 完全兼容 |

## 性能提升

Rust 版本相比 Go 版本的优势：

- 🚀 **启动速度更快**：无运行时开销
- 💾 **内存占用更少**：无 GC 开销
- 🔒 **类型安全**：编译期检查，减少运行时错误
- 📦 **单二进制**：无需外部依赖

## 卸载 Go 版本（可选）

如果决定完全迁移到 Rust 版本，可以卸载 Go 版本：

```bash
# 卸载 Go 版本
cyber-zen uninstall

# 或手动删除
sudo rm -f /usr/local/bin/cyber-zen
```

## 回退方案

如果需要回退到 Go 版本：

1. 从 GitHub 重新下载 Go 版本
2. 或从源码重新构建 Go 版本

配置文件在两个版本之间完全兼容，无需担心数据丢失。

## 常见问题

### Q: 构建失败怎么办？

**A:** 检查以下几点：
1. Rust 版本是否 >= 1.70
2. 网络连接是否正常（需要下载依赖）
3. 系统依赖是否完整（如 macOS 需要 Xcode Command Line Tools）

```bash
# 更新 Rust
rustup update

# 检查 Rust 环境
rustup show
```

### Q: 命令找不到？

**A:** 确保已正确安装：
```bash
# 检查安装路径
which cyber-zen

# 检查 PATH 环境变量
echo $PATH
```

### Q: 配置文件找不到？

**A:** 确保配置文件在正确位置：
```bash
# 检查配置文件位置
ls -la configs/
ls -la ~/.cyber-zen/configs/
```

### Q: 性能没有提升？

**A:** 确保使用发布版本：
```bash
# 使用发布版本构建
cargo build --release

# 使用发布版本运行
cargo run --release -- status
```

## 技术支持

如遇到问题，请：
1. 检查本文档的常见问题部分
2. 查看项目 README.md
3. 提交 Issue 到项目仓库

## 迁移检查清单

- [ ] 安装 Rust 环境
- [ ] 构建 Rust 项目
- [ ] 测试所有命令
- [ ] 安装到系统
- [ ] 验证配置文件兼容性
- [ ] 测试实际工作流
- [ ] （可选）卸载 Go 版本

迁移完成后，即可享受 Rust 版本带来的性能和安全性提升！

