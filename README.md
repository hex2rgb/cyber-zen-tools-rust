# Cyber Zen Tools (Rust 版本)

一个简洁跨平台命令行工具集，专注于开发工作流优化。使用 Rust 重写，提供更好的性能和类型安全。

## ✨ 特性

- 🚀 **快速 Git 操作**: 一键提交和推送，支持智能 commit message 生成
- 🖼️ **图片压缩**: 智能压缩图片文件，保持质量
- 🎨 **彩色输出**: 清晰的状态反馈
- 🔧 **简单安装**: 一键安装到系统
- 📦 **跨平台**: 支持 macOS 和 Linux
- 🛠️ **开发友好**: 完整的构建和开发工具链
- 🔗 **集成管理**: 内置卸载功能
- 🤖 **智能分析**: 自动分析代码变更并生成规范的 commit message
- ⚙️ **灵活配置**: 支持多种编程语言和文件类型的智能识别
- ⚡ **高性能**: Rust 实现，无运行时开销，启动更快

## 🚀 快速开始

### 环境要求

- Rust 1.70+ （[安装指南](https://www.rust-lang.org/tools/install)）
- Git（用于 gcm 命令）
- 配置文件（可选，会自动查找）

### 安装

#### 从源码构建

```bash
# 克隆项目
git clone <repository-url>
cd cyber-zen-tools/cyber-zen-tools-rust

# 构建项目
cargo build --release

# 安装到系统
cargo install --path .
# 或手动安装
sudo cp target/release/cyber-zen-tools /usr/local/bin/cyber-zen
```

#### 验证安装

```bash
cyber-zen --version
cyber-zen --help
```

### 使用

```bash
# 查看帮助
cyber-zen --help

# 查看版本
cyber-zen --version

# Git 提交和推送（自动生成 commit message）
cyber-zen gcm

# Git 提交和推送（手动指定 message）
cyber-zen gcm "手动指定的提交信息"

# 压缩图片
cyber-zen compress --src "images/" --rate 0.7

# 启动静态文件服务器
cyber-zen server --port 3000

# 查看状态
cyber-zen status

# 卸载程序
cyber-zen uninstall
```

## 📋 命令说明

### `gcm` - Git 提交和推送（规则版）

基于规则和配置文件的智能 commit message 生成。

```bash
cyber-zen gcm [message]
```

**功能特性**:
- **智能分析**: 自动分析 Git 变更内容
- **文件识别**: 支持 50+ 种编程语言和文件类型
- **智能分类**: 基于文件路径和类型自动分类
- **规范生成**: 生成符合 Conventional Commits 规范的 commit message
- **用户确认**: 提供交互式确认和编辑选项

**使用方式**:
1. **自动生成**: `cyber-zen gcm` - 程序自动分析变更并生成 commit message
2. **手动指定**: `cyber-zen gcm "message"` - 使用用户指定的提交信息

**执行流程**:
1. `git add .` - 添加所有变更
2. 生成智能 commit message（如果未指定）
3. `git commit -m "message" --no-verify` - 提交变更
4. `git push` - 推送到远程仓库

**智能分析示例**:
```bash
$ cyber-zen gcm

未提供提交信息，正在自动分析变更...
 检测到 Git 变更...

📁 文件变更状态:
  ✨ 新增: src/components/UserProfile.tsx
  🔧 修改: src/pages/Home.tsx
  🗑️  删除: src/components/OldComponent.tsx

 变更统计:
  新增文件: 1 个
  修改文件: 1 个
  删除文件: 1 个
  总变更: 3 个文件

 生成的 Commit Message:
feat: 重构用户界面并优化用户体验

- 新增 UserProfile 用户资料管理组件
- 优化 Home 首页布局和交互体验
- 清理废弃的 OldComponent 组件

是否使用此消息? [Y/n]
```

### `compress` - 图片压缩

```bash
cyber-zen compress --src "源文件或文件夹" --dist "目标路径" --rate "压缩比率"
```

**压缩策略**：
1. 优先保证图片质量（无损压缩）
2. 按指定比率缩小图片尺寸
3. 自动优化文件大小

**支持的格式**：
- JPEG (.jpg, .jpeg): 质量优化 + 尺寸调整
- PNG (.png): 无损压缩 + 尺寸调整
- GIF (.gif): 尺寸调整
- 其他格式: 直接复制

**特性**：
- 自动添加时间戳避免覆盖
- 保持原文件扩展名
- 支持相对路径和绝对路径
- 自动创建目标目录
- 支持递归处理目录

**参数**：
- `--src`: 源文件或文件夹路径（必需）
- `--dist`: 目标路径（可选，默认当前目录）
- `--rate`: 压缩比率 0.1-1.0（可选，默认0.8）

**示例**：
```bash
# 压缩文件夹
cyber-zen compress --src "images/" --dist "compressed/" --rate 0.7

# 压缩单个文件
cyber-zen compress --src "photo.jpg" --rate 0.5

# 使用默认设置
cyber-zen compress --src "photos/"
```

### `server` - 静态文件服务器

```bash
cyber-zen server [目录] [选项]
```

**功能**：
- 启动简单的静态文件服务器
- 类似于 Python 的 `http.server`
- 支持目录浏览
- 请求日志记录

**参数**：
- `目录`: 服务目录（可选，默认当前目录）
- `-p, --port`: 端口号（可选，默认3000）

**示例**：
```bash
# 在当前目录启动服务器，端口 3000
cyber-zen server

# 在指定目录启动服务器，端口 8080
cyber-zen server ./public --port 8080

# 在指定目录启动服务器，端口 5000
cyber-zen server /path/to/dir -p 5000
```

### `status` - 查看工具状态

```bash
cyber-zen status
```

显示：
- 安装目录
- 版本信息
- 平台信息
- Git 和 Bash 可用性

### `uninstall` - 卸载程序

```bash
cyber-zen uninstall
```

从系统中卸载程序，删除 `/usr/local/bin/cyber-zen` 文件。

## ⚙️ 配置文件

### 配置文件类型

Cyber Zen Tools 使用 TOML 格式的配置文件，支持以下三种类型：

#### 1. **文件类型配置** (`file-types.toml`)
- **前端开发**: React, Vue, Svelte, TypeScript, JavaScript, CSS, SCSS, Less, Stylus, HTML, SVG 等
- **后端开发**: Go, Python, Java, C/C++, C#, Rust, PHP, Ruby, Node.js 等
- **数据库**: SQL, SQLite, 配置文件等
- **文档**: Markdown, 脚本文件等
- **构建部署**: 依赖管理, Docker, 配置文件等

#### 2. **文件分类配置** (`categories.toml`)
- **测试文件**: test, tests, spec 等目录
- **文档文件**: doc, docs, documentation 等目录
- **源代码**: src, app, lib, packages 等目录
- **配置文件**: config, conf, settings 等目录
- **脚本文件**: script, scripts, bin, tools 等目录
- **资源文件**: assets, static, public 等目录
- **数据库文件**: migrations, db, models 等目录
- **部署文件**: deploy, docker, k8s 等目录

#### 3. **Commit 模板配置** (`commit-templates.toml`)
- **变更类型**: feat, fix, refactor, style, docs, test, chore, perf, cleanup 等
- **中文描述**: 各种变更类型的中文说明
- **动作词**: 新增、优化、删除、重命名等
- **智能规则**: 基于变更类型的自动判断逻辑

### 配置文件位置

配置文件统一存放在 `~/.cyber-zen/configs/` 目录，安装脚本会自动创建并复制默认配置。

### 自定义配置

用户可以修改配置文件来自定义：
- 新增文件类型支持
- 调整文件分类规则
- 自定义 commit message 模板
- 添加新的编程语言支持

## 🔧 开发

### 环境要求
- Rust 1.70+
- Cargo（Rust 包管理器）
- Git

### 构建

```bash
# 构建程序（开发模式）
cargo build

# 构建程序（发布模式，优化）
cargo build --release

# 运行程序
cargo run -- --help

# 运行测试
cargo test

# 清理构建目录
cargo clean
```

### 项目结构

```
cyber-zen-tools-rust/
├── Cargo.toml              # Rust 项目配置
├── src/
│   ├── main.rs            # 程序入口
│   ├── commands/          # 命令模块
│   │   ├── mod.rs
│   │   ├── gcm.rs         # Git 提交命令
│   │   ├── compress.rs    # 图片压缩命令
│   │   ├── server.rs      # 静态服务器命令
│   │   ├── status.rs      # 状态显示命令
│   │   └── uninstall.rs   # 卸载命令
│   └── config/            # 配置管理
│       ├── mod.rs
│       ├── config.rs      # 配置加载
│       └── filetypes.rs   # 文件类型管理
├── configs/               # 配置文件目录（TOML 格式）
│   ├── file-types.toml
│   ├── categories.toml
│   └── commit-templates.toml
└── README.md
```

### 依赖说明

主要依赖：
- `clap` - 命令行参数解析
- `serde` / `toml` - 配置序列化（TOML 格式）
- `colored` - 终端颜色输出
- `image` - 图片处理
- `regex` - 正则表达式
- `chrono` - 时间处理
- `candle-core` / `candle-nn` / `candle-transformers` - AI 模型推理（gcm-ai 功能）
- `tokenizers` - 文本分词
- `anyhow` - 错误处理

## 🚀 性能优势

相比 Go 版本，Rust 版本提供：

- ⚡ **启动速度**: 无运行时开销，启动更快
- 💾 **内存占用**: 无 GC，内存占用更少
- 🔒 **类型安全**: 编译期检查，减少运行时错误
- 📦 **单二进制**: 静态链接，无需外部依赖
- 🛡️ **内存安全**: Rust 的所有权系统保证内存安全

## 📚 文档

- [快速开始](./QUICKSTART.md) - 快速上手指南
- [Qwen 模型设置](./QWEN_SETUP.md) - AI 模型配置指南
- [下一步计划](./NEXT_STEPS.md) - 开发路线图

## 🤝 贡献

欢迎贡献代码！请遵循以下步骤：

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

## 许可证

Apache License 2.0

## 版本历史

### v1.0.0 (Rust 版本)
- ✅ 完全重构为 Rust
- ✅ 保持所有功能兼容
- ✅ 性能优化
- ✅ 类型安全提升

---

**注意**: 这是 Rust 重构版本，与 Go 版本功能完全兼容，配置文件可共享使用。

